import { FluenceLevelPipe } from './fluence-level.pipe';

describe('FluenceLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new FluenceLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
